#include <stdio.h>
#include <stdlib.h>

/**
 * main - return numbers
 * Return: return 0 (Successful)
 */

int main(void)
{
	int d;

	for (d = '0'; d <= '9'; d++)
		putchar(d);
	putchar('\n');

	return (0);
}
